# CLAUDE.md

LLM 이 코딩할 때 흔히 저지르는 실수를 줄이기 위한 행동 지침.

출처: https://github.com/multica-ai/andrej-karpathy-skills/blob/main/CLAUDE.md
1~4 는 그 문서의 내용이고, 5 는 이 저장소에만 해당하는 규칙이다.

**절충:** 이 지침은 속도보다 신중함 쪽으로 기운다. 사소한 작업에는 판단해서 쓸 것.

## 1. 코딩 전에 생각하기

**넘겨짚지 말 것. 헷갈리는 것을 숨기지 말 것. 트레이드오프를 드러낼 것.**

구현하기 전에:
- 전제를 명시적으로 말한다. 확신이 없으면 묻는다.
- 해석이 여러 개면 전부 제시한다 — 말없이 하나를 고르지 않는다.
- 더 간단한 방법이 있으면 말한다. 근거가 있으면 반대 의견을 낸다.
- 불명확하면 멈춘다. 무엇이 헷갈리는지 짚고 묻는다.

## 2. 단순함이 먼저

**문제를 푸는 최소한의 코드. 넘겨짚은 것은 넣지 않는다.**

- 요청하지 않은 기능을 넣지 않는다.
- 한 번만 쓰는 코드에 추상화를 만들지 않는다.
- 요청하지 않은 "유연성"이나 "설정 가능함"을 넣지 않는다.
- 일어날 수 없는 상황에 대한 예외 처리를 넣지 않는다.
- 200줄을 썼는데 50줄로 될 것 같으면 다시 쓴다.

스스로 물을 것: "시니어 엔지니어가 이걸 보고 과하다고 할까?" 그렇다면 단순화한다.

## 3. 수술하듯 고치기

**꼭 건드려야 하는 것만 건드린다. 내가 낸 것만 치운다.**

기존 코드를 고칠 때:
- 옆에 있는 코드·주석·서식을 "개선"하지 않는다.
- 망가지지 않은 것을 리팩터링하지 않는다.
- 내 취향과 달라도 기존 스타일에 맞춘다.
- 관련 없는 죽은 코드를 발견하면 말만 한다 — 지우지 않는다.

내 변경이 고아를 만들었다면:
- 내 변경 때문에 안 쓰이게 된 import·변수·함수를 지운다.
- 원래부터 죽어 있던 코드는 요청받지 않는 한 지우지 않는다.

판정 기준: 바뀐 모든 줄이 사용자의 요청으로 곧장 이어져야 한다.

## 4. 목표로 굴러가게 하기

**성공 기준을 정한다. 확인될 때까지 돈다.**

작업을 확인 가능한 목표로 바꾼다:
- "검증 추가" → "잘못된 입력에 대한 테스트를 쓰고, 통과시킨다"
- "버그 수정" → "재현하는 테스트를 쓰고, 통과시킨다"
- "X 리팩터링" → "전후로 테스트가 통과하는지 확인한다"

여러 단계짜리 작업은 짧게 계획을 말한다:

```
1. [단계] → 확인: [무엇으로]
2. [단계] → 확인: [무엇으로]
3. [단계] → 확인: [무엇으로]
```

성공 기준이 강하면 혼자 돌 수 있다. 약한 기준("되게 해줘")은 계속 되물어야 한다.

---

**이 지침이 먹히고 있다는 신호:** diff 에 불필요한 변경이 줄고, 과하게 만들어 다시 쓰는 일이 줄고,
확인 질문이 실수 뒤가 아니라 구현 전에 나온다.

---

## 5. 이 저장소의 규칙

위 원칙을 이 프로젝트에 적용할 때 실제로 걸렸던 것들이다.

### 확인 없이 「됐다」고 하지 않기

이 프로젝트에서 확인은 **빌드해서 눈으로 보는 것**이다. 컴파일만으로는 부족하다.
실제로 컴파일은 통과하는데 화면에 아무것도 안 나온 적이 여러 번 있다.

```bash
Tools/Build-SnailPet.ps1
```

**유니티를 직접 batchmode 로 부르지 말 것.** 에디터가 열려 있거나 에셋 변경 후 첫 실행이면
빌드가 조용히 아무것도 안 하는데 로그에 `error CS` 가 안 나온다. 이 스크립트는 그 둘을
처리하고, 산출물이 소스보다 새로운지로 성공을 판정한다.

### 데이터는 xlsx 가 원본

`SnailData.xlsx` 를 고쳤으면 반드시 다시 생성한다. 생성 코드를 직접 고치면 다음 생성에서 날아간다.

```bash
dotnet run --project Tools/DataPipeline
```

검증에서 오류가 나면 **아무 파일도 쓰지 않고 멈춘다.** 그건 고장이 아니라 설계다.
`Tools/DataPipeline/IdMap.json` 은 토큰→ID 매핑이고 세이브 데이터와 묶인 신원이다.
지우거나 번호를 재배치하지 말 것. 새 토큰은 뒤에 붙기만 한다.

### 스프라이트 없는 Image 는 빈 칸이 아니다

UGUI 에서 `Image.sprite` 가 null 이면 **색으로 꽉 찬 사각형**이 그려진다.
이 함정을 세 번 밟았다 — 아이콘이 까만 사각형, 선택 테두리가 빨간 덩어리, 빈 칸이 회색 덩어리.

프리팹에는 **런타임에 만든 스프라이트가 저장되지 않는다.** 코드로 생성한 도형을 쓰는 곳은
`UiShapeRef` 로 역할을 남겨 두어야 살아날 때 다시 채워진다.

### UI 는 프리팹이 원본

`Assets/Resources/Ui/SnailUi.prefab` 이 실제로 화면에 나오는 것이다.
코드 빌더(`SnailUi.Build`)는 프리팹을 굽는 용도다.

빌더를 고쳤으면 **프리팹을 다시 구워야 반영된다** (메뉴 `SnailPet → 5. UI 프리팹 생성`).
다시 굽면 손으로 옮긴 배치가 사라지므로, 사용자가 편집한 흔적이 있으면 먼저 물어볼 것.

### .ps1 은 UTF-8 BOM 이 필요하다

여기 PowerShell 5.1 은 BOM 없는 `.ps1` 을 ANSI 로 읽는다. 한글이 깨지고 엉뚱한 구문
오류가 난다. 편집 도구가 BOM 을 떼므로 고칠 때마다 다시 붙여야 한다.

반대로 생성한 `.cs` 는 BOM 이 있어야 하고(한글 문자열), JSON 은 BOM 이 있으면 안 된다.
